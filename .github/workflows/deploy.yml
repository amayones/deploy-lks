steps:
- name: Deploy
uses: appleboy/ssh-action@v1
with:
host: ${{ secrets.HOST }}
username: ec2-user
key: ${{ secrets.SSH_KEY }}
script: |
cd lks-cloud-computing-2026
git pull origin main
npm install
pm2 restart all
